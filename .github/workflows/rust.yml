name: Rust

on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
        matrix:
            os: [ubuntu-latest]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Rust Toolchain
        run: rustup toolchain install stable --profile minimal -c rustfmt,clippy

      - name: Clippy
        run: cargo clippy

      - name: Build
        run: cargo build --verbose

      - name: RAPL test 1
        run: ls -la /dev/cpu/*/msr
        
      - name: RAPL test 2
        run: ls -la /dev/cpu/

      - name: Tests
        run: cargo test --verbose
